const express = require("express");

// other files

// global objects
const server = express();
const port = 4001;

// middleware

// request handlers

// GET /api/users

// GET /api/users/:id

// POST /api/users

// DELETE /api/users/:id

// PUT /api/users/:id
